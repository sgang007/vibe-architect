You are building on Replit. Use the Replit environment — all dependencies should be installable via npm/pip. The backend should run on port 8080 (Replit default). Use environment secrets for API keys.

{{ content }}
