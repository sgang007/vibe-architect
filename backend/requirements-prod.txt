# ── Vibe Architect — Production dependencies ──────────────────────────────────
# Stripped of NLP/ML libraries (ENABLE_NLP_LAYER=false in production).
# This keeps the Docker image small (~400 MB vs 3+ GB with PyTorch + models).
# ──────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.115.*
uvicorn[standard]==0.30.*
python-multipart==0.0.*

# Pydantic + settings
pydantic==2.8.*
pydantic-settings==2.4.*

# HTTP client (for LLM adapters)
httpx==0.27.*

# LLM SDKs
anthropic>=0.34
openai==1.45.*

# Template engine
jinja2==3.1.*

# Redis (optional — only needed if SESSION_BACKEND=redis)
redis[asyncio]==5.1.*

# Utilities
python-dotenv==1.0.*
